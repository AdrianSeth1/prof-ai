import PlaceholderScreen from './PlaceholderScreen'

export default function LiveLecture() {
  return (
    <PlaceholderScreen
      title="Live Lecture"
      detail="Browser-side mic capture · WebSocket audio channel · faster-whisper transcription · state machine (Lecture → Awaiting → Processing → Speaking) · live gap panel · Q&A history"
      phase="Phase 5"
    />
  )
}
